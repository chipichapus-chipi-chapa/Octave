<Window x:Class="kurs_musik_club.windows.TeacherAndCourse"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kurs_musik_club.windows"
        mc:Ignorable="d"
        Title="Управление курсами и преподавателями" Height="750" Width="1000"
        Background="#121212">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление курсами и преподавателями" 
                 Foreground="White" 
                 FontSize="24" 
                 FontWeight="Bold"
                 Margin="20,20,0,20"/>

        <TabControl Grid.Row="1" Margin="20">
            <TabItem Header="Курсы">
                <Grid>
                    <!-- Content for Courses tab -->
                </Grid>
            </TabItem>
            <TabItem Header="Преподаватели">
                <Grid>
                    <!-- Content for Teachers tab -->
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window> 