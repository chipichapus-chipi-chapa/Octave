<Window x:Class="kurs_musik_club.windows.registration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kurs_musik_club.windows"
         Title="Регистрация в Octave" 
        Height="830" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#121212">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooltoVis" />

        <!-- Стиль для текстовых полей (адаптированный под главную страницу) -->
        <Style x:Key="OctaveTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Padding" Value="0,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="4" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="10,12"/>
                            <TextBlock Text="{TemplateBinding Tag}" 
                                       Foreground="#9E9E9E"
                                       Margin="10,0"
                                       VerticalAlignment="Center"
                                       FontSize="{TemplateBinding FontSize}"
                                       Visibility="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF5722"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопки регистрации -->
        <Style x:Key="OctaveRegisterButton" TargetType="Button" BasedOn="{StaticResource MaterialButton}">
            <Setter Property="Background" Value="#FF5722"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,20,0,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3" Color="#FF5722"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Левая часть с изображением -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Width="400" Background="#1A1A1A">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="/img/tg.jpg"  Margin="0,10,0,10" HorizontalAlignment="Center" Height="318" Width="332"/>
                <TextBlock Text="Присоединяйтесь к музыкальному сообществу" 
                           Foreground="#9E9E9E"
                           FontSize="18"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Width="300"/>
            </StackPanel>
        </Border>

        <!-- Правая часть с формой -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel  Margin="60,10">
                <Image Source="/img/octave-logo.png" Width="109" Margin="0,10,0,10" HorizontalAlignment="Left" Height="70"/>

                <TextBlock Text="Создать аккаунт" 
                           Foreground="White" 
                           FontSize="28" 
                           FontWeight="Bold"
                           Margin="0,0,0,30"/>


                <StackPanel>
                    <TextBox x:Name="txtName" 
                             Tag="Полное имя" 
                             Style="{StaticResource OctaveTextBox}"
                             />

                    <TextBox x:Name="txtEmail" 
                             Tag="Email" 
                             Style="{StaticResource OctaveTextBox}"/>

                    <TextBox x:Name="txtPassword" 
                                Tag="Пароль" 
                                Style="{StaticResource OctaveTextBox}"/>

                    <TextBox x:Name="txtConfirmPassword" 
                                Tag="Подтвердите пароль" 
                                Style="{StaticResource OctaveTextBox}"/>

                    <Button x:Name="btn_reg" Content="ЗАРЕГИСТРИРОВАТЬСЯ" 
                            Style="{StaticResource OctaveRegisterButton}" Click="btn_reg_Click"
                            />

                    <TextBlock x:Name="txt_sign_in" Text="Уже есть аккаунт? Войти" 
                               Foreground="#9E9E9E"
                               HorizontalAlignment="Center"
                               Margin="0,20,0,0"
                               Cursor="Hand" MouseLeftButtonDown="txt_sign_in_MouseLeftButtonDown">
                        <TextBlock.InputBindings>
                            <MouseBinding Gesture="LeftClick" Command="{Binding LoginCommand}"/>
                        </TextBlock.InputBindings>
                    </TextBlock>
                </StackPanel>

                <!-- Социальные сети -->
                <StackPanel Margin="0,22,0,0">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        
    </Grid>
</Window>