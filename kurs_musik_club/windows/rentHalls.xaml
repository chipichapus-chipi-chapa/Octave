<Window x:Class="kurs_musik_club.windows.rentHalls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kurs_musik_club.windows"
        mc:Ignorable="d"
        Title="Аренда зала" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#121212">
    <Window.Resources>
        <!-- Стиль для заголовков -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="20,20,20,10"/>
        </Style>

        <!-- Стиль для подзаголовков -->
        <Style x:Key="SubHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9E9E9E"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Стиль для ComboBox -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Стиль для Calendar -->
        <Style x:Key="CalendarStyle" TargetType="Calendar">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Стиль для кнопок -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF5722"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для текстовых блоков с информацией -->
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Аренда зала" 
                   Style="{StaticResource HeaderStyle}"
                   HorizontalAlignment="Center"/>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель с выбором зала -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Выберите зал:" 
                           Style="{StaticResource SubHeaderStyle}"/>
                
                <ComboBox x:Name="cmbHalls" 
                          Style="{StaticResource ComboBoxStyle}"
                          DisplayMemberPath="hall_name"
                          SelectedValuePath="id_hall"
                          SelectionChanged="cmbHalls_SelectionChanged"/>

                <TextBlock Text="Выберите дату:" 
                           Style="{StaticResource SubHeaderStyle}"/>
                
                <Calendar x:Name="dpDate" 
                         Style="{StaticResource CalendarStyle}"
                         SelectedDatesChanged="dpDate_SelectedDateChanged"/>

                <TextBlock Text="Выберите время:" 
                           Style="{StaticResource SubHeaderStyle}"/>
                
                <ComboBox x:Name="cmbTime" 
                          Style="{StaticResource ComboBoxStyle}"
                          SelectionChanged="cmbTime_SelectionChanged"/>
            </StackPanel>

            <!-- Разделитель -->
            <Rectangle Grid.Column="1" 
                       Width="2" 
                       Fill="#333333" 
                       Margin="0,20"/>

            <!-- Правая панель с информацией о зале -->
            <StackPanel Grid.Column="2" Margin="20,0,0,0">
                <TextBlock x:Name="txtHallName" 
                           Text="Название зала" 
                           Style="{StaticResource InfoTextStyle}"
                           FontSize="20"/>
                
                <TextBlock x:Name="txtHallDescription" 
                           Text="Описание зала" 
                           Style="{StaticResource InfoTextStyle}"/>

                <TextBlock x:Name="txtHallCapacity" 
                           Text="Вместимость: X человек" 
                           Style="{StaticResource InfoTextStyle}"/>

                <Border Background="#1E1E1E" 
                        CornerRadius="5" 
                        Padding="10">
                    <Image x:Name="imgHall" 
                           Height="200" 
                           Stretch="UniformToFill"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Кнопки внизу -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20">
            <Button x:Name="btnBook" 
                    Content="Забронировать" 
                    Style="{StaticResource ButtonStyle}"
                    Click="btnBook_Click"/>
            <Button x:Name="btnCancel" 
                    Content="Отмена" 
                    Style="{StaticResource ButtonStyle}"
                    Background="#333333"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>