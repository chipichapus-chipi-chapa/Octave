<Window x:Class="kurs_musik_club.windows.CoursesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kurs_musik_club.windows"
        mc:Ignorable="d"
        Title="Курсы" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Поиск курсов:" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
            <TextBox x:Name="SearchTextBox" 
                     TextChanged="SearchTextBox_TextChanged"
                     Padding="5"
                     FontSize="14"
                     BorderThickness="1"
                     BorderBrush="#CCCCCC"
                     Background="White">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="CoursesListView" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="700">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="{Binding ImagePath}" Width="80" Height="80" Stretch="Uniform"/>
                        
                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Text="{Binding TeacherName}" Margin="0,5,0,0"/>
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{Binding Price, StringFormat='{}{0:C}'}" 
                                 VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window> 