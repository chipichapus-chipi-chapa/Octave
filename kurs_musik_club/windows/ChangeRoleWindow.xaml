<Window x:Class="kurs_musik_club.windows.ChangeRoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Изменение роли пользователя" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#121212">

    <Window.Resources>
        <Style x:Key="RoleButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="20" 
                                    Height="20" 
                                    Stroke="{TemplateBinding Foreground}"
                                    StrokeThickness="2">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{TemplateBinding Foreground}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <ContentPresenter Margin="10,0,0,0"
                                            VerticalAlignment="Center"/>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SaveButton" TargetType="Button">
            <Setter Property="Background" Value="#FF5722"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,20,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Выберите новую роль пользователя" 
                       Foreground="White" 
                       FontSize="20" 
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>
            
            <RadioButton x:Name="clientRole" 
                         Content="Клиент"
                         Style="{StaticResource RoleButton}"
                         IsChecked="True"/>
            
            <RadioButton x:Name="teacherRole" 
                         Content="Преподаватель"
                         Style="{StaticResource RoleButton}"/>
            
            <Button x:Name="saveButton"
                    Content="Сохранить"
                    Style="{StaticResource SaveButton}"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 